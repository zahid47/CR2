import { Container } from "react-bootstrap";

function Title() {
	return (
		<Container>
			<h1 className="text-center font-weight-bold title-padding">
				Get a simplified class routine
			</h1>
			<h5 className="text-center">Valid for SWE Spring-21</h5>
		</Container>
	);
}

export default Title;
